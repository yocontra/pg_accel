Let me be the one to say it, because the author's own `CLAUDE.md` already says it louder than I can: **"NEVER add CPU fallbacks. GPU execution is the entire purpose of this library. If a GPU kernel crashes or fails, the fix is to make the GPU path work — not to add a CPU fallback that bypasses it. CPU fallbacks are test cheats that hide real bugs."** Good. Now open `benchmarks/README.md` and count the 1.00x rows. `hashagg_100g | 1.00x | 1.00x | 1.00x | 0.43x | 0.21x` (line 86). `h3_bulk | 1.00x | 1.00x | 1.00x | 1.00x | 1.00x` (line 137). `gpu_sort_topk_wide | 1.00x | 1.00x | 1.00x | 1.01x | 1.00x` (line 91). The overall geomean is **"0.89x"** (line 66) — i.e. a GPU-accelerated Postgres extension, benchmarked against its own parent, is on average *slower than doing nothing*. Per rule 11 this matrix is structurally impossible: either those kernels dispatched and tied the CPU (in which case they are worthless), or the planner silently skipped them (in which case the cost model is a lie). Pick one. Both are bugs. And `pg_accel_stats()` ships a `cpu_fallback_count` column (`src/engine/stats.rs:29`) specifically to catch this — so either the counter is zero and the above paragraph is an unexplained performance regression, or the counter is nonzero and the author shipped a benchmark that its own self-assertion would reject.

Second, the SSBM row. `ssbm | 65 | 0.95x | 4 / 65` (line 64). Sixty-five SSBM measurements, four of them statistically significant, and the geomean is *below unity*. Look at `ssbm_q1_1 | 0.95x | 0.98x | 1.01x | 0.53x | 0.29x` (line 166) — at 10M rows, the "GPU-accelerated" path runs SSBM Q1.1 at **29% of PG-parallel speed**. SSBM is the canonical star-schema OLAP benchmark; it is the thing you wave around on a homepage when you are building a GPU database. If your first-table-in-the-README headline number for SSBM is 0.95x, you are not building an OLAP accelerator, you are building a very expensive Postgres proxy. Meanwhile `window_row_number | 0.99x | 0.86x | 0.46x | 0.58x | 0.29x` (line 160) — a 3.4x slowdown at 10M — despite the project advertising `gpu.window.*` tracing spans in its own diagnostics table. Either those spans never fired (planner skip, cost model broken) or they fired and the GPU window kernel loses to a CPU `row_number()` by 3.4x. There is no charitable reading.

Third, and this is the one I would screenshot: grep the tree. `/Users/contra/Projects/pg_accel/pg_accel/src/gpu/three_layer.rs`, function `try_gpu_dispatch`, line 226: `if geoms_a.iter().any(is_degenerate) || geoms_b.iter().any(is_degenerate) { return None; }`. One degenerate polygon in the batch short-circuits the **entire batch** to `None`, which the caller interprets as "mark all pairs uncertain, let PG recheck handle it." Now look at `vsweep_50kv | 0.99x | 1.00x | 0.22x | 0.08x | 1.00x` (line 127) and `vsweep_100kv | 1.00x | 1.00x | 0.22x | 0.08x | 1.00x` (line 128). At 1M rows, 100K-vertex polygons, the "GPU benchmark" runs at **8% of PG-parallel speed**. That is not a GPU benchmark. That is a measurement of PostGIS doing the work twice: once through the pg_accel bbox dance and once through the recheck path, because `try_gpu_dispatch` bailed on the first row with a not-quite-closed ring. And the file where this happens is still literally named `src/gpu/fallback.rs` (which exports `pgaccel_cpu_fallback_count`, `src/gpu/fallback.rs:359`), despite the project's whole marketing being "no CPU fallbacks." Refactor cost: zero. Rhetorical damage: terminal. You cannot ship a benchmark report whose headline geomean is 0.89x, whose canonical OLAP suite is 0.95x, whose window kernels lose by 3.4x, whose reduce category is **"gpu_reduce | 36 | 0.50x"** (line 58), and whose spatial flagship is 8% of baseline because a degenerate-input guard in a file named `fallback.rs` is quietly routing everything back to PostGIS. Fix the planner gate, fix the degenerate-geom kernel, delete `fallback.rs` or rename it, and re-run the suite before anyone sees it.

*"I would, respectfully, not put this on a homepage."*
